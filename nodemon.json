{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "TS_NODE_BASEURL=./src npx ts-node -r tsconfig-paths/register --transpile-only src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
